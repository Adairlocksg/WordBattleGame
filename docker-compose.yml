version: '3.8'

name: 'wordbattle'

services:
  db:
    image: postgres:15-alpine
    container_name: wordbattle
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    ports:
      - '5432:5432'
    volumes:
      - .data:/var/lib/postgresql/data
